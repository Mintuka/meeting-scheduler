# Backend service environment for local/dev. Copy to `docker.env` and fill in.

# --- SMTP (optional; leave blank to disable email sending) ---
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
FROM_EMAIL=
APP_NAME=Meeting Scheduler (Local)

# --- IMAP for email reply listener (optional) ---
IMAP_SERVER=imap.gmail.com
IMAP_PORT=993

# --- Google OAuth / Calendar API (required for real Meet links) ---
# Obtain these from Google Cloud Console (same project as your OAuth client):
# - Enable Google Calendar API
# - OAuth consent screen: External (Testing) + add Test users
# - OAuth Client (Web): set redirect URI to http://localhost:8000/api/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:8000/api/google/callback

# Default timezone for events (IANA name or UTC)
DEFAULT_TIMEZONE=UTC

# --- Database (optional override; compose sets these in dev) ---
# MONGODB_URL=mongodb://admin:password@mongodb-dev:27017/meeting_scheduler?authSource=admin
# MONGODB_DATABASE=meeting_scheduler

# Note: Do NOT commit docker.env. Use this file (docker.env.example) for sharing.
