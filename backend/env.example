# Database Configuration
MONGODB_URL=mongodb://localhost:27017
DATABASE_NAME=meeting_scheduler

# Server Configuration
PORT=8000
FRONTEND_URL=http://localhost:3000

# Email Notification Configuration
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=mintuka2015@gmail.com
SMTP_PASSWORD=yxli riqf kmkw kzmk
FROM_EMAIL=mintuka2015@gmail.com
APP_NAME=Meeting Scheduler

# Optional: Use environment variables for sensitive data
# SMTP_PASSWORD can also be set as an environment variable
# For Gmail, you'll need to generate an App Password
# Go to Google Account > Security > 2-Step Verification > App passwords

# Google OAuth / Calendar API
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:8000/api/google/callback
GOOGLE_PROJECT_ID=meeting-scheduler

# JWT / Auth
JWT_SECRET_KEY=change-me
ACCESS_TOKEN_EXPIRE_MINUTES=60

# AI / LLM Configuration
# AI Provider: "gemini", "openai", or "anthropic"
AI_PROVIDER=gemini
# API Key for your chosen provider
# For Gemini, you can use either GEMINI_API_KEY or AI_API_KEY
GEMINI_API_KEY=your-gemini-api-key-here
AI_API_KEY=your-api-key-here
# Model to use
# For Gemini: "gemini-1.5-flash", "gemini-1.5-pro", "gemini-pro"
# For OpenAI: "gpt-4o-mini", "gpt-4", "gpt-3.5-turbo"
# For Anthropic: "claude-3-haiku-20240307", "claude-3-opus-20240229"
AI_MODEL=gemini-1.5-flash
# Optional: Custom base URL for API (useful for proxies or self-hosted)
# AI_BASE_URL=https://api.openai.com/v1
