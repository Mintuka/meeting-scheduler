services:
  backend-dev:
    env_file:
      - docker.env
    environment:
      # MONGODB_URL and MONGODB_DATABASE are loaded from docker.env
      # Using MongoDB Atlas cloud database instead of local MongoDB
